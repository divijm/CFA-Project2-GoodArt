<h1>Artists</h1>

<p>These magicians craft their genius for you all!</p>


<!--

<div class="container gallery">
  <div class="row">
    <% @arts.each do |art| %>
      <div class= "col-sm-3">
        <div class="card">
          <div class="panel">
            <%= image_tag art.image.url(:thumb) %>
            <div class="caption"><%= art.title %><%= art.description %><%= art.price %>&amp; Image Foreground: FFFFFF</div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div> -->
<!--
    <table class="table table-hover">
      <thead>
        <tr>
          <th>Image</th>
          <th>Title</th>
          <th>Description</th>
          <th>Price</th>
          <th>Size</th>
          <th>Medium</th>
          <th>Location</th>
          <th>Date made</th>
          <th>User</th>
          <th>Rating</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @arts.each do |art| %>
          <tr>
            <td><%= image_tag art.image.url(:thumb) %></td>
            <td><%= art.title %></td>
            <td><%= art.description %></td>
            <td><%= art.price %></td>
            <td><%= art.size %></td>
            <td><%= art.medium %></td>
            <td><%= art.location %></td>
            <td><%= art.date_made %></td>
            <td><%= art.user.email %></td>
            <td><%= rating_for art, 'rating', star: 5 %></td>
            <td><%= link_to 'Show', art %></td>
            <td><%= link_to 'Edit', edit_art_path(art) %></td>
            <td><%= link_to 'Destroy', art, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table> -->
